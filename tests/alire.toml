name = "tests"
description = "Tests for the built-in Alire test runner"
version = "0.0.0-test"

[[depends-on]]
rand = "*"
rand_core = "*"
rand_distributions = "*"

[[pins]]
rand = { path = ".." }
rand_core = { path = "../rand_core" }
rand_distributions = { path = "../rand_distributions" }

[build-switches]
"*".ada_version = "Ada2022"
"*".contracts = "Yes"

[build-profiles]
rand  = "development"
rand_core = "development"
rand_distributions = "development"
tests = "development"
