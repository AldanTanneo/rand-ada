name = "tests"
description = "Tests for the built-in Alire test runner"
version = "0.0.0-test"

[[depends-on]]
rand = "*"

[[pins]]
rand = { path = ".." }

[build-switches]
"*".ada_version = "Ada2022"

[build-profiles]
rand  = "validation"
tests = "validation"

[[pins]]
rand_distributions = { path='../rand_distributions' }
